# NeoForge mod metadata (generated from templates; values expanded from gradle.properties)

modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"
logoFile="logo.png"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="Trycky"
description='''
Adds /rtp (random teleport) for NeoForge servers.
Includes a per-player cooldown and safe location search.
'''
# Optionnel (recommandé pour release)
# displayURL="https://github.com/TON_USER/TonRepo"
# issueTrackerURL="https://github.com/TON_USER/TonRepo/issues"
# credits="Contributors / testers"

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"
